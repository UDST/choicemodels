Run the tests from the repository root with `pytest`. Add `-s` to see printed output, or `--cov` for a coverage report.

The estimation tests compare against fixed reference values generated with `urbansim.urbanchoice` (UrbanSim 3.2), so UrbanSim does not need to be installed.
